# 定时函数<body中>
* 每隔1min触发

```html
<script>
    function xxx() {
        $.get('/../', function (res) {
            if (res.code == -1) {

            } else if (res.code) {
                XXX函数(res.msg(参数1), '参数2');
            } else {
                XXX2函数(res.msg(参数1), '参数2', '参数3'...);
            }
        })
    }
    setInterval(xxx, 6000);
</script>
```
