from django.contrib.auth import authenticate
user = authenticate(username='xxx', password='xxxxx')
login(request, user)
logout(request)

@login_required
@login_required(redirect_field_name='url_name')
@login_required(login_url='/xxx/xxx')

if request.user.is_authenticated:


redirect('xxx/xx')
redirect(reverse('url_name'))


from django.contrib.auth.models import AbstractUser


class User(AbstractUser):
    telephone = models.CharField('ÊÖ»úºÅ', max_length=11)
    nickname = models.CharField('êÇ³Æ', max_length=16)